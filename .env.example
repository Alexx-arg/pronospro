# =============================================================================
# Football Prediction App - .env.example
# Backend + Frontend. Elegir el provider según tu cuenta (bzzoiro o API-Football).
# =============================================================================

# ---------- App ----------
ENV=development
LOG_LEVEL=INFO
BACKEND_API_PREFIX=/api/v1
ADMIN_API_KEY=tumasterkey
# API_SECRET_KEY=clave-generada-aqui                   # vacío = se genera al azar
CORS_ORIGINS=*

# ---------- PostgreSQL ----------
POSTGRES_USER=football
POSTGRES_PASSWORD=changeme
POSTGRES_DB=football
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://football:changeme@postgres:5432/football
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=5
DB_POOL_TIMEOUT=30
DB_ECHO=false
DB_APP_ROLE=app_user
DB_APP_ROLE_PASSWORD=changeme

# ---------- Data Provider (API-Football — actual, trae métricas xG/córners/tarjetas) ----------
DATA_PROVIDER=api_football
API_FOOTBALL_KEY=your_api_football_key_here
API_FOOTBALL_BASE_URL=https://v3.football.api-sports.io
API_FOOTBALL_RATE_LIMIT_PER_MIN=30
API_FOOTBALL_RATE_LIMIT_PER_DAY=100

# ---------- NVIDIA NIM Explanation Service ----------
NVIDIA_API_KEY=tu_nvidia_key_aqui
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
NVIDIA_MODEL=meta/llama-3.2-11b-vision-instruct

# ---------- Scheduling ----------
SCHEDULER_ENABLED=false
SYNC_UPCOMING_ENABLED=true
SYNC_FINISHED_ENABLED=true
SYNC_TEAMS_ENABLED=true
SYNC_TEAM_STATISTICS_ENABLED=true
SYNC_PLAYER_STATISTICS_ENABLED=true
SYNC_INJURIES_ENABLED=true
SYNC_LINEUPS_ENABLED=true
CURRENT_LEAGUES=61,140
CURRENT_SEASON_YEAR=2026

# ---------- Prediction ----------
PREDICTION_DATASET_VERSION=v001
PREDICTION_CONFIDENCE_BUCKETS=0.4,0.5,0.6,0.7,0.8,0.9,1.0
PREDICTION_RANDOM_SEED=42
PREDICTION_MAX_GOALS=10

# ---------- Frontend (Flutter) ----------
# BACKEND_BASE_URL=http://192.168.1.5:8000             # esto apunta a tu PC (desarrollo)
# BACKEND_BASE_URL=https://tu-app.onrender.com        # esto será  la URL de Render cuando esté
# API_KEY=                                            # vacío = no handler auth